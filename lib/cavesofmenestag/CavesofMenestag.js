/**
 * CavesofMenestag.js -- File to hold the CavesofMenestag class.
 */

class CavesofMenestag {
	/**
	 * Main constructor for the CavesofMenestag class.
	 */
	constructor(config) {
		let self = this;

		self.config = config;
	}
};

module.exports = CavesofMenestag;